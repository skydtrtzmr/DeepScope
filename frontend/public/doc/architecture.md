---
title: 数据架构
---

## 节点展开机制

后端全量返回邻居节点和边，前端负责去重。

### 数据流

1. 前端发送 `expandGraph(nodeId, m, n)` 请求
2. 后端 BFS 遍历，返回所有邻居节点和边（可能包含已在画布上的节点）
3. 前端通过 `existingNodeIds` 过滤重复节点
4. 前端通过 `nodeIdSet` 确保边的两端节点都在画布上
5. 增量渲染到 G6 图谱

### 分页加载

当直接邻居数超过 `m` 时，可以通过 "加载更多" 分页获取剩余邻居。
